# я сподіваюсь я правильно зрозуміла таск

ФУНКЦІЯ generate_pattern(n):
    # робимо масив a[n][n], заповнений "|"
    ДЛЯ i В ДІАПАЗОНІ 0 ДО n-1:
        ДЛЯ j В ДІАПАЗОНІ 0 ДО n-1:
            a[i][j] = "|"

    ДЛЯ i В ДІАПАЗОНІ 0 ДО n-1:
        ЯКЩО n є парним:
            ЯКЩО i == 0 АБО i == n-1:  # перший і останній рядок
                a[i][0] = "*"  # перше значення
                a[i][n-1] = "*"  # останнє значення
            ІНАКШЕ:  # внутрішні рядки
                a[i][0] = "|"  # перше значення
                a[i][n-1] = "|"  # останнє значення
                ДЛЯ j В ДІАПАЗОНІ 1 ДО n-2:  # внутрішні символи
                    a[i][j] = "*"
        ІНАКШЕ:  # якщо n непарне
            ДЛЯ j В ДІАПАЗОНІ 0 ДО n-1:
                ЯКЩО i == j АБО (i + j) == n - 1:  # дві діагоналі
                    a[i][j] = "*"

    # виводимо масив
    ДЛЯ кожного рядка row В a:
        ВИВОДИ рядок приєднуючи пробіли між символами


# питаємо у користувача розмір масиву
n = ВВІД("Введіть розмір масиву (N): ")
ЯКЩО n <= 0:
    ВИВОДИ "N повинно бути додатнім цілим числом."
ІНАКШЕ:
    generate_pattern(n)
